SENDLOG — CHANGELOG
===================
A log of all features, fixes, and improvements grouped by push.


──────────────────────────────────────────────────────────────
PUSH 1  |  6437ed9  |  Initial commit
──────────────────────────────────────────────────────────────
- Initial project scaffold (React + Vite)


──────────────────────────────────────────────────────────────
PUSH 2  |  f3fdc64  |  Add user save features
──────────────────────────────────────────────────────────────
- User accounts: sign up, log in, sign out
- Supabase kv_store used to persist all user data
- Sessions, climbs, and projects saved per user


──────────────────────────────────────────────────────────────
PUSH 3  |  23112bb  |  Core app build-out
──────────────────────────────────────────────────────────────
- Full climbing session tracking (start, log climbs, end session)
- Active session timer with live clock
- Climb form: name, grade, scale, tries, completed/project flag, comments, photo, color, wall types, hold types
- Session logbook in profile (filterable by scale, grade, gym, sort order)
- Projects tab: active, completed (sent), and retired projects
- Stats tab: send count, flash rate, best grade, total attempts, session count
- Grade breakdown cards and bar chart with time-frame selector (2W / 1M / 6M / 1Y / All)
- Preferred grading system setting (V-Scale, Font, YDS, etc.)
- Location dropdown with known gyms + ability to hide/remove locations
- Tag chips for wall type and hold type on climb cards
- Session summary screen shown at end of each session
- Calendar view of sessions by month
- Bottom navigation: Home, Session, Profile


──────────────────────────────────────────────────────────────
PUSH 4  |  f621d68  |  UI updates and new features
──────────────────────────────────────────────────────────────
- UI polish across cards, buttons, and spacing
- Additional stat cards and layout improvements


──────────────────────────────────────────────────────────────
PUSH 5  |  6dbdaba  |  Fix all-time chart + filter stats by bar
──────────────────────────────────────────────────────────────
- Fixed all-time line chart to correctly span full session history
- Clicking a bar/point on the chart now filters all stat cards to show data for that period only
- Selected bar label shown above the stat cards


──────────────────────────────────────────────────────────────
PUSH 6  |  920d13b  |  Stats rearrangement and new cards
──────────────────────────────────────────────────────────────
- Fixed pill/chip spacing across the stats tab
- Date-range labels added to stat cards (e.g. "Past 2 Weeks")
- Calendar toggle button moved to a cleaner position in stats
- New stat cards: Best Day (climbs), Active Projects, Projects Sent


──────────────────────────────────────────────────────────────
PUSH 7  |  56a4a1d  |  Calendar gym colors + grade pie chart
──────────────────────────────────────────────────────────────
- Calendar dots now colored by gym/location (each gym gets a unique color)
- Sessions dropdown on calendar shows sessions for the selected month
- Grade pie chart added to stats tab: shows grade distribution of completed climbs
- Pie chart is filterable by grading scale
- Pie slices are clickable to highlight and filter


──────────────────────────────────────────────────────────────
PUSH 8  |  90f60a8  |  Pie chart fix + new stat cards
──────────────────────────────────────────────────────────────
- Fixed pie chart angle bug (slices were rendering incorrectly)
- Added Average Rest Days stat (days between sessions)
- Added Average Tries per climb stat card


──────────────────────────────────────────────────────────────
PUSH 9  |  c95a84a  |  Rest tracking + bigger pie chart
──────────────────────────────────────────────────────────────
- Time between attempts is now tracked and displayed
- Pie chart made larger with a bottom legend showing grade + count
- Rest-related stat cards added to the stats overview


──────────────────────────────────────────────────────────────
PUSH 10  |  0b79f4a  |  Social tab — search, follow, feed
──────────────────────────────────────────────────────────────
- New Social tab in bottom navigation
- Search for other users by username or display name
- Follow / Unfollow any user
- Social feed tab: see sessions from people you follow combined with your own


──────────────────────────────────────────────────────────────
PUSH 11  |  7b6ed83  |  Follower/following counts + combined home feed
──────────────────────────────────────────────────────────────
- Follower and Following counts shown on your own profile
- Combined home feed: your own sessions + followed users' sessions sorted by date
- Sessions from followed users show a "posted by" label
- Social sessions in home feed show full pie chart and are tappable (read-only)


──────────────────────────────────────────────────────────────
PUSH 12  |  c2a7a33  |  User profile screen
──────────────────────────────────────────────────────────────
- Tapping a user in search, follower list, or feed opens their full profile
- User profile shows their stats, recent sessions, and projects (read-only)
- Follow / Unfollow button on user profile
- Profile accessible from social search, follower/following modal, and feed cards


──────────────────────────────────────────────────────────────
PUSH 13  |  08c25f5  |  Social bug fixes and polish
──────────────────────────────────────────────────────────────
- Fixed typing bug: search input was losing focus after every character (caused by JSX re-mounting)
- Fixed following list not persisting across sessions (was reading stale cached data)
- Search results now show stats beside each user (sessions, climbs, best grade)
- Home feed social sessions now show the full pie chart correctly


──────────────────────────────────────────────────────────────
PUSH 14  |  86a0d74  |  Tappable follower/following on other profiles
──────────────────────────────────────────────────────────────
- Follower and Following counts on other users' profiles are now tappable
- Opens a list of their followers or following with the option to view each profile
- Consistent with how it works on your own profile


──────────────────────────────────────────────────────────────
PUSH 15  |  5fa38ee  |  Color themes + social moved to profile + UI tweaks
──────────────────────────────────────────────────────────────
- Three color themes added, selectable in Settings:
    Espresso  ☕  — warm dark (default)
    Alpine    🏔  — light natural
    Chalk     🎨  — colorful warm
- Theme preference saved to your profile and restored on login
- Social moved from bottom nav to a button (👥) on the profile screen header
- Follower/Following count pills made smaller and more compact
- Fixed LocationDropdown syntax error from previous session


──────────────────────────────────────────────────────────────
PUSH 16  |  72c99f8  |  Social follow UX fixes
──────────────────────────────────────────────────────────────
- Blocked following yourself: search results, user profiles, and follower/following modals all show a "You" badge instead of a follow button
- Followers modal now shows "Follow Back" button for users you don't follow yet, and a "Following" label for those you do
- Follower/Following count pills made even smaller on your own profile
- Stats chart axis labels made bolder for better readability


──────────────────────────────────────────────────────────────
PUSH 17  |  b4811f1  |  Mutual follow, notifications, remove follower, unfollow confirm
──────────────────────────────────────────────────────────────
- "Follows you" badge shown on user profiles when they follow you back
- Unfollow now requires two taps to confirm (shows "Unfollow?" with Cancel / Confirm) — works in user profiles, search results, and the following modal
- Follow notifications: when someone follows you, a notification is stored; a badge appears on the Profile nav button showing unread count; notifications are listed in your profile screen with an unread dot indicator; all marked as read when you open the profile
- Remove follower: your own Followers list now has a Remove button per user — removes them from your followers and removes you from their following list
- Home feed empty state: brand new users with no sessions see a friendly prompt with a "Find Climbers" CTA instead of a blank screen


──────────────────────────────────────────────────────────────
PUSH 18  |  e96f39d  |  Reactions, photos, notif bell, mute, private accounts, notif prefs
──────────────────────────────────────────────────────────────
- Notification bell (🔔) fixed in the home screen header with an unread count badge; tapping opens a full notification panel showing both follow notifications and session activity notifications
- Photos section in session detail: any climbs with photos are shown in a horizontal scrollable strip with a colored grade badge overlaid at the bottom and climb name in the corner
- Reactions on social feed cards: tap 🔥, 💪, or ✨ to react to someone's session; your reaction is highlighted and persisted in your own kv store
- Mute user button on other users' profiles: hides their sessions from your home feed and social feed without unfollowing them; tap again to unmute
- Private account toggle in Settings: when enabled, users who don't follow you see a 🔒 lock screen instead of your sessions and stats
- Notification preferences in Settings: two toggle switches to independently enable/disable follow notifications and session activity notifications
- Session activity notifications: automatically generated when followed users post new sessions within the last 24 hours; de-duplicated so you never see the same session twice


──────────────────────────────────────────────────────────────
PUSH 19  |  16d68d6  |  Notifications tab, comments, reaction counts, private follow requests
──────────────────────────────────────────────────────────────
- Notifications moved to Social tab (Feed tab removed — followed sessions stay on Home only)
- Notifications tab shows full list with × dismiss button per item
- 🔔 bell panel on Home screen also gets per-item × dismiss buttons
- Reaction counts (🔥💪✨) now visible to everyone via aggregate kv store (sessionReactions:{id})
- Counts displayed inline next to each emoji on feed cards
- Comment button (💬) added to every feed card; tapping opens a comment panel (bottom sheet)
- Comment panel shows existing comments and lets you post a new one
- Private accounts: tapping Follow sends a follow request instead of following immediately
- Follow button shows "Requested" state while request is pending
- Follow request inbox appears on your Profile screen when requests are waiting; Accept / Deny per request
- Accepting updates both sides' follower/following data automatically
- All new state (pendingFollowRequests, myFollowRequests, feedReactionCounts, comments) persists across sessions and resets on logout
- Location dropdown fix: clicking ▼ now immediately shows all location options (was broken due to click bubbling)
- New location "Save" button moved inline next to the text input (no longer at bottom of dropdown)


──────────────────────────────────────────────────────────────
PUSH 20  |  7eade8e  |  Comment notifications, reactions on profiles, notification polish
──────────────────────────────────────────────────────────────
- Comment notifications: posting a comment sends a notification to the session owner
- Delete your own comments: × button appears on your own comments in the comment panel
- Reactions (🔥💪✨) and comment button added to recent sessions on other users' profile pages
- Comments section added to session detail screen (read-only view): shows preview of up to 3 comments and a "Comment" button to open the full panel
- Follow request notifications: requesting to follow a private account sends them a notification
- Notification types expanded: comment and followRequest types shown with correct copy in all notification UIs
- Notification badge removed from Profile nav button — notifications now accessed only via Social tab or bell
- Unread badge moved to the Notifications tab label in the Social screen; tapping marks all as read
- Profile screen no longer clears notification count on open


──────────────────────────────────────────────────────────────
PUSH 21  |  5be63ca  |  Session card bar chart, photo strip, border polish
──────────────────────────────────────────────────────────────
- Replaced pie chart on home feed session cards with a horizontal bar chart showing attempts per grade, colored by grade
- Photo strip added to session cards: climbs with photos appear as a horizontal scrollable row of 72×72 thumbnails, each with a grade badge overlaid in the bottom-left corner
- Session card border made more prominent: thicker border using accent color with a subtle glow box-shadow for better contrast against the background
- Profile screen notifications list made collapsible: click the "Notifications" header to expand/collapse, collapsed by default to reduce clutter


──────────────────────────────────────────────────────────────
PUSH 22  |  c0a7613  |  Rope/speed climb types, lightbox nav, bar legend, pagination
──────────────────────────────────────────────────────────────
New climb types:
- "New Climb" button renamed to "New Boulder"; session action buttons now a 2×2 grid
- New Rope Climb button: opens a form with hold color, French/YDS grade picker, and Top Rope vs Lead style selector
- New Speed Climb button: opens a form with optional name, time entry in seconds, and completed toggle
- All climb types share the same name, comments, photo, and project-toggle fields where applicable

Photo improvements:
- Climb photos in card strips and session detail are now tappable to open fullscreen lightbox
- Lightbox supports prev/next navigation (◀▶ buttons) when a session has multiple photos; shows photo count (e.g. 2/4)
- Hold color circle indicator added to photo thumbnails (bottom-right corner) in both card strips and session detail photos

Session card:
- Bar chart now shows a two-tone fill: bright = sends, dim = remaining attempts per grade
- Bar chart label changed from raw tries to sends/attempted (e.g. 2/3)
- Legend row added below bar chart explaining the two-tone colour coding

Feed & logbook:
- Logbook sessions tab now paginated (8 per page) with a "Load more" button showing remaining count


──────────────────────────────────────────────────────────────
PUSH 30  |  pending  |  Rope takes, boulder fall UX, session timers, profile redesign, signup seed

Rope climbing:
- Rope log form (after "Done") now has separate Falls and Takes counters
- Takes are tracked independently on each route (climb.takes field)
- Attempt summary bar shows attempts / falls / takes / TOPPED

Boulder climbing:
- Fall counter only visible when an attempt is actively in progress (climbingStartedAt set)
- Fall counter has only a + button (no minus) — tap + to log a fall and end the attempt
- "Mark Sent" button is always visible regardless of attempt state

Session type timers:
- Boulder/rope section timers now run continuously between attempts of that type
- Timer only pauses when you switch to the other type or manually end the section
- Previously, each attempt pause would stop the type timer (now fixed)

New account signup:
- New accounts are automatically seeded with ~1 year of realistic climbing data
- Beginner progression: starts at VB/V0, builds to V2-V5 range over the year
- Replaces the old static SAMPLE_SESSIONS/SAMPLE_PROJECTS demo data

Profile screen redesign:
- Following and follower counts moved inline below username (small tappable pill buttons)
- Settings and Social buttons replaced by a 3-button row: Settings / Social / Leaderboard
- Leaderboard button is placeholder (disabled) for future implementation
- Notifications panel removed from profile screen (notifications still in nav bar)


──────────────────────────────────────────────────────────────
PUSH 29  |  pending  |  Session button layout, boulder pause, new themes

Session active screen:
- "New Boulder" button moved inside the boulder section (below the climbs list)
- "New Rope Climb" button moved inside the rope section (below the climbs list)
- Before a section is started, the button still appears in the main action area
- "Log Project" button always visible in the bottom action area

Boulder pause/resume:
- After at least one attempt, a pause (⏸) button appears next to the rest timer
- Tapping it pauses attempt tracking: rest timer hidden, "Paused" state shown
- "Resume" button re-activates attempt tracking on that climb

App themes — 3 new themes added (6 total):
- Neon: pure black background with neon cyan highlights
- Midnight: dark navy blue, easy on the eyes, cool tone
- Ember: dark charcoal with warm amber accents, easy on the eyes
- Theme picker updated to a 3x2 grid with descriptions


──────────────────────────────────────────────────────────────
PUSH 28  |  pending  |  Rope climb timer rework, grouped layout, logbook type filter

Rope climb timer rework:
- Tapping "Start Attempt" starts a live wall timer for how long you're on the route
- Tapping "Done" stops the timer and opens an inline form: enter falls + topped checkbox
- "Log Attempt" saves the attempt (+1 try, adds falls, sets completed)
- Rope climbs now track attempts (tries) and falls separately — old data is backward-compatible

Active session layout:
- Boulder climbs grouped under the Boulder session card with a left border accent
- Rope climbs grouped under the Rope session card with a left border accent
- Starting an attempt on any climb clears the rest timer on all other climbs

Rest recommendations:
- After 3+ minutes of rest since the last attempt, the card shows a "Ready?" prompt

Logbook session type filter:
- New "Type" pill row in the sessions filter panel: All / Boulder / Rope / Speed / Mixed
- Sessions are classified by the types of climbs they contain
- "Active" badge on the filter toggle reflects the type filter

Rope falls tracking:
- Logbook ClimbRow shows "X attempts · Y falls" for rope climbs
- Session summary climbs list shows "X attempts · Y falls" for rope climbs
- Stats tab rope stats: "Total Falls" and "Avg Falls/Route" now use actual falls (not attempt count)
- "Best Day Falls" in rope stats also uses actual falls


──────────────────────────────────────────────────────────────
PUSH 27  |  pending  |  Activity-based timers, falls model, discard session
──────────────────────────────────────────────────────────────
Activity-based boulder/rope timers:
- Boulder/rope session timers now only tick when a climb of that type is actively being worked on
- Timer shows ACTIVE (green) / PAUSED / ENDED badge — pausing is automatic
- Mutual exclusion: starting a boulder climb auto-pauses rope timer (and vice versa)
- Timer accumulates correctly across multiple active/rest periods

Falls model (boulder):
- Boulder "tries" renamed to "falls" throughout (active session cards, logbook, summary)
- Flash = 0 falls when marked sent (previously tried === 1)
- Auto-increment removed: marking sent no longer adds a try automatically

Discard session:
- After ending a session, "Discard Session" button appears on the summary screen
- Discarding removes the session from the logbook and reverts any project completions

Related improvements:
- Auto-stop climb timer when the Edit form is opened for a climb
- Attempt times (chips) shown on logbook ClimbRow cards
- Per-climb attempt time chips shown in Session Summary climbs list
- BoulderRopeSessionCard shows Active/Paused/Ended visual state badges


──────────────────────────────────────────────────────────────
PUSH 26  |  pending  |  Climb type session timers + per-climb attempt tracking
──────────────────────────────────────────────────────────────
Session timers (boulder + rope):
- Boulder Session and Rope Session timer cards appear when first climb of each type is added
- Live elapsed timer and End button on each card (matches Speed Session UX)

Per-climb attempt timing:
- "Start Climbing" / "Start Attempt" button on each boulder/rope climb card
- Live "Climbing ⏱ X:XX" timer while climbing; stops when a try is logged or sent
- Rest timer shown between attempts ("Resting X:XX")
- Attempt log below each card (#1 0:45 · #2 1:12)

Session summary — time by type:
- "Time by Type" card: boulder time, rope time, speed time in one breakdown

Stats tab improvements (related improvements #1, #4, #5):
- Speed chart: falls shown as red ✗ markers at the bottom
- Speed PB History list: shows each PB with date logged
- Stats category tab persists across sessions (localStorage)


──────────────────────────────────────────────────────────────
PUSH 25  |  pending  |  Stats category tabs: Overall, Boulder, Rope, Speed
──────────────────────────────────────────────────────────────
- Four category tabs at top of stats: Overall, Bouldering, Rope, Speed
- Overall: broad overview — all existing stats, chart, calendar, grade breakdown, pie chart
- Bouldering: filtered stats (sends, flash rate, best grade, avg tries, projects, rest) + grade breakdown
- Rope: filtered stats (routes sent, falls, onsight rate, best grade, avg falls/route) + grade breakdown
- Speed: PB hero card, stat cards (total time, tops, falls, success rate, sessions, avg per session), attempt timeline chart with PB line
- Bar chart interactions (click to filter stats) work independently per category
- Empty state shown when no data exists for a type in the selected time frame


──────────────────────────────────────────────────────────────
PUSH 24  |  pending  |  Speed session timer, photo cards, rope labels, speed stats
──────────────────────────────────────────────────────────────
Speed session card:
- Session elapsed timer shown at top-left of every SpeedSessionCard (live while active, frozen when ended)
- "End" button in card header: freezes the timer and hides Add Attempt / rest timer
- "ENDED" badge shown when session is finished
- Multiple speed sessions labeled "Speed Session 1", "Speed Session 2", etc.

Session summary screen:
- Speed Climbing stats block added: total time in speed sessions, total attempts, best time

Stats tab:
- Speed PB stat card added (only shown when speed attempts exist in the selected time frame)

Active climb cards:
- Photo thumbnail shown at top-left of card if a photo exists; tap to open fullscreen lightbox; no overlay on thumbnail
- Rope climb style badge ("🔝 TR" or "🧗 Lead") shown on active climb card
- Rope climb try counter label changed from "tries/try" to "falls/fall"


──────────────────────────────────────────────────────────────
PUSH 23  |  8f71469  |  Session type selector, speed sessions, lightbox swipe
──────────────────────────────────────────────────────────────
Session setup:
- New type selection prompt before starting: choose Bouldering, Rope Climbing, and/or Speed Climbing (multi-select)
- At least one type must be selected; selection remembered for the session

Session active screen:
- Action buttons filtered to selected climb types; Log Project always shown
- "See More" toggle reveals unselected type buttons without cluttering the main view
- Header sent/total count excludes speed sessions

Speed Climb Session (replaces old "New Speed Climb" form):
- Tapping "Speed Climb Session" immediately adds a speed session card to the active session
- Card shows a live rest timer counting up since the last attempt (ticks every second)
- "Add Attempt" opens an inline form: enter time in seconds, or tap "Fell"
- Attempts listed with ✓ time / ✗ Fell and rest gap shown between entries
- Best time highlighted with a PB badge
- Speed sessions are excluded from regular climb stats (sends, grade breakdown, bar chart)
- End Session confirmation shows speed attempt count separately

Lightbox:
- Touch swipe left/right to navigate between photos (in addition to tap buttons)
